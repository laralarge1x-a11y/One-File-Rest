# Generated by `cap add android` — kept out of version control because it's
# large, machine-specific, and reproducible from `capacitor.config.ts`.
/android/

# Generated by `vite build && cap copy` — the bundled web shell.
/www/

# Native build outputs
*.apk
*.aab

# Keystore (NEVER commit a release keystore)
*.keystore
*.jks

node_modules/
